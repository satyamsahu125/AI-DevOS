You are an independent Principal Software Architect, QA Architect,
AI-Agent Systems Engineer, and DevOps Engineer.

You are auditing the entire AI DevOS repository.

DO NOT modify any source code yet.

Your job is to independently discover whether the current architecture
actually implements the documented behavior.

Repository:
F:\AI-DevOS3

Read and analyze EVERYTHING relevant:

- backend/
- frontend/
- app/
- agents/
- workflow/
- execution/
- review/
- memory/
- context/
- actions/
- workspace/
- sandbox/
- tests/
- configuration
- documentation
- PRD/TRD/specification files
- generated artifacts
- templates
- agent registration
- stage definitions
- validators
- dependency handling
- retry handling
- approval gates

Do not trust filenames, comments, documentation, or previous AI claims.

Verify implementation against implementation.

Do not assume that because a stage reports "approved" that it
actually passed validation.

Do not assume that because a test suite exists that tests actually
execute.

Do not assume that build=True means the application is correct.

Do not assume that tests=0/0 means success.

Trace the complete execution path from:

User Request
→ Clarification
→ Requirements
→ PRD
→ Domain Research
→ QA
→ Architecture
→ Technology Selection
→ Strategic Review
→ Sprint Planning
→ File Planning
→ Backend
→ Frontend
→ Testing
→ Validation
→ Review
→ Deployment

For every stage determine:

1. What enters the stage?
2. What context does it receive?
3. What artifact does it produce?
4. Who validates it?
5. What happens if validation fails?
6. What later stages consume it?
7. Is the dependency explicit or implicit?
8. Can the stage run with incomplete information?
9. Can the stage contradict an earlier decision?
10. Can the stage silently assume missing requirements?

Pay special attention to:

- agent registry integrity
- stage → agent mappings
- stage ordering
- retry policies
- deterministic vs transient failures
- validation gates
- reviewer behavior
- artifact dependencies
- context assembly
- memory correctness
- technology compatibility
- frontend/backend compatibility
- mobile/web compatibility
- test execution
- sandbox execution
- dependency detection
- dependency pinning
- generated code validation
- API contract preservation
- frontend API consumption
- QA coverage
- E2E coverage
- false-positive approvals
- stages marked successful despite validation failures
- missing agents
- dead agents
- unreachable stages
- stages that can execute without prerequisites
- artifacts that can be overwritten after approval
- stale artifacts
- inconsistent terminology
- duplicate responsibilities
- undocumented behavior
- undocumented assumptions

Analyze the recent failure:

DependencyException:
agent sprintdelta is not registered

Determine whether the fix is actually architectural or merely
a patch.

Also investigate this result:

sandbox:
lint_errors=86
tests=0/0
build=True
install=True

Determine whether the system incorrectly treats this as successful.

Also investigate:

reviewer:
cross-stage gap
uncovered_modules=['Presentation','Business','Data']

Determine why the reviewer can still approve the stage.

Also investigate:

FrontendDeveloper generated:
frontend/src/App.jsx

Determine whether the system had enough information to know whether
the requested application was:

- web
- mobile
- web + mobile

If the answer is no, identify exactly where clarification should
have blocked downstream development.

Produce:

AUDIT_FINDINGS.md

with:

CRITICAL
HIGH
MEDIUM
LOW
ARCHITECTURAL DEBT
DESIGN DEBT
TESTING DEBT
DOCUMENTATION DEBT

For every finding provide:

ID
Severity
Subsystem
Evidence
Root Cause
Impact
Current Behavior
Expected Behavior
Recommended Fix
Affected Files
Required Tests
Regression Risk

Do not modify code.

Do not invent missing behavior.

If something cannot be verified, mark it UNVERIFIED.