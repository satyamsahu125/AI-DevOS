fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
PyYAML>=6.0.0
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
hnswlib>=0.8.0
sentence-transformers>=3.0.0
transformers>=4.0.0
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0  # JWT tokens (R8 auth)
bcrypt>=4.0.0                      # password hashing (R8 auth) — used directly, no passlib
docker>=6.1.0
tenacity>=8.2.3
boto3>=1.34.0

# Phase 6 — Infrastructure & Production Hardening
alembic>=1.13.0             # database migration tracking (Phase 6 baseline)
celery>=5.3.0               # task queue (replaces daemon threads when Redis is available)
redis>=5.0.0                # Celery broker + result backend
psycopg2-binary>=2.9.0      # PostgreSQL driver (optional — for Postgres memory store)
structlog>=24.0.0           # structured logging (optional — system uses stdlib if absent)
opentelemetry-sdk>=1.24.0                         # distributed tracing
opentelemetry-instrumentation-fastapi>=0.45b0     # FastAPI auto-instrumentation
prometheus-fastapi-instrumentator>=6.1.0          # Prometheus /metrics endpoint for FastAPI

# NOTE: pyyaml is listed above and remains a required dependency.
# The Phase 6 spec states "make removal explicit" under the assumption pyyaml was
# already optional, but this is incorrect: pyyaml is actively imported in
#   app/config/loader.py:14  (import yaml as _yaml)
#   app/execution/file_validator.py:103  (import yaml)
# Removing it would break both modules. This is a spec/code mismatch; pyyaml stays.

# sqlite3 is part of the Python standard library -- not installed via pip.
